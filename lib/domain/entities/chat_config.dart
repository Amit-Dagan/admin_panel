/// Represents configuration settings for the ChatGPT integration.
class ChatConfig {
  /// The model identifier, e.g. "gpt-3.5-turbo", "gpt-4".
  final String model;

  const ChatConfig({required this.model});
}